apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(":JavaUtility")
}

sourceCompatibility = java_version
targetCompatibility = java_version
